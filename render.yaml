services:
  - type: web
    name: anime-md
    runtime: node
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.19.5
    disk:
      name: anime-md-auth-data
      mountPath: /var/data
      sizeGB: 1
